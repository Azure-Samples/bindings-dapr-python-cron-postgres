name: bindings-dapr-node-postgres-aca
metadata:
  template: bindings-dapr-node-postgres-aca@0.0.1-beta
services:
   api:
    project: batch
    language: python
    host: containerapp
    module: app/api
 